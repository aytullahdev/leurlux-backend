'use strict';

/**
 * protraining-request router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::protraining-request.protraining-request');
