'use strict';

/**
 * protraining-request service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::protraining-request.protraining-request');
