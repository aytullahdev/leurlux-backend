'use strict';

/**
 * transport-request service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::transport-request.transport-request');
