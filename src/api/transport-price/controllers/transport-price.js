'use strict';

/**
 * transport-price controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::transport-price.transport-price');
